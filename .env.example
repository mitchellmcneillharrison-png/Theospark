# Theospark API configuration (Groq).
# SERVER-SIDE only — used by the Vercel function in api/chat.js and never
# exposed to the browser, so no VITE_ prefix.
#
# Locally: copy to .env.local and run `vercel dev`.
# On Vercel: set this under Settings -> Environment Variables.
GROQ_API_KEY=
